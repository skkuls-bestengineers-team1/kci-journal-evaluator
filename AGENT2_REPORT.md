# Agent2 구현 및 테스트 결과

Agent2(게재논문의 학술적 가치와 성과, 54점 만점)의 구현 내용과 합성데이터 테스트 결과를 정리한 문서입니다.

## 1. 구현 내용

### 파일
| 파일 | 내용 |
|---|---|
| `agents/agent2_academic_value.py` | Agent2 본체 (스키마, 프롬프트, 점수 환산, 노드 함수) |
| `agent2_test.py` | Agent2 단독 테스트 (Original/Synthetic A·B·C 4개 PDF) |

### 동작 방식
Agent3와 같은 패턴입니다.
1. `state["paper"]["raw_text"]`와 `state["journal_meta"]`의 `major_research_field` / `middle_research_field`를 읽는다. 분야 값이 `None`이면 빈 문자열로 처리한다.
2. Gemini(`config.GEMINI_MODEL_NAME`, temperature 0)가 3개 세부항목의 A~F 등급과 근거(rationale)만 생성한다 (`with_structured_output`).
3. 점수는 코드에서 계산한다: `config.GRADE_TO_RATIO` × `config.ACADEMIC_VALUE_SUBSCORES`를 합산한 뒤 54점 기준으로 정규화한다.
4. `{"stage3_results": [result]}`를 반환한다 (`Annotated[list, add]` reducer가 Agent3 결과와 병합).

### 세부항목과 배점
원본 4개 항목 중 "편집위원회 활동"(논문 원문으로 판단 불가)은 제외하고, 나머지 3개를 54점으로 비례 재배점했다 (`config.py`).

| 세부항목 | 원배점 | 재배점 | 판단 기준 |
|---|---|---|---|
| `recognition` (인지도·활용도) | 3 | ≈ 9.5 | 신청요강 착안점 2 |
| `suitability` (KCI 등재지로서의 학술적 가치·적합성) | 10 | ≈ 31.8 | 신청요강 착안점 1 |
| `topic_fit` (학술지 주제 부합도) | 4 | ≈ 12.7 | 신청요강 착안점 3 |

등급별 비율: A 100% / B 90% / C 80% / D 70% / E 60% / F 0%

### 프롬프트 문장별 근거
| 구분 | 내용 |
|---|---|
| 신청요강 원문 | 착안점 1·2·3에 해당하는 각 항목의 핵심 기준 문장 (3개) |
| 프로젝트 결정 반영 | 편집위원회 활동 제외 (`config.py` 재배점 결정) |
| 합성데이터 문서에서 일반화 | suitability의 "연구 설계와 분석 근거", "결론의 과장 여부" |
| 추론 (원문 근거 없음) | 외부 인용 수 등 추정 금지, A~F 등급 설명, 평가 원칙 (Agent3 문구 재사용), 내용/형식 구분 섹션 |

## 2. 테스트 방법
- 데이터: `data/`의 Original(`Real_Paper.pdf`, 로컬 전용) + Synthetic A/B/C
- 학술지 분야: 공학 / 전자·정보통신공학 (`agent2_test.py`의 `JOURNAL_META`)
- 각 조건 3회 반복 (temperature 0이어도 실행마다 결과가 달라짐)
- 실행: `python agent2_test.py`
- 기대 패턴 (`data/README_합성데이터_설계_v2.md`): Agent2 `Original ≈ B > A ≈ C`, Agent3 `Original ≈ A > B ≈ C`

### 합성데이터에서 실제로 바뀐 것 (Original 대비 직접 비교)
| 데이터 | 변경 내용 | Agent2 기대 |
|---|---|---|
| A | 실험 절에 "소수 사례 임의 선정, 표본 규모 미고정, 반복 실험·통계 검정 없음, 평가자 일치도 미확인, 지표 정의 미제시" 등을 명시. 초록·결론의 "우수성 입증"은 그대로 | 하락 |
| B | 실험 설계·수치는 동일. 캡션 축약, 절 제목 변경, 문단·결론 반복, 절 순서 뒤섞기 등 형식만 변경 | Original과 유사 |
| C | A + B | A와 유사 |

## 3. 테스트 결과

### 최종 결과 (Agent2 + Agent3, 각 3회, 3단계 만점 80 / 과락선 56)
| | Agent2 (54점) | Agent3 (26점) | 3단계 합계 |
|---|---|---|---|
| Original | 54.0 (AAA) | 22.8 (CBA) | **76.8** |
| Synthetic A | 45.5 (44.5~47.6) | 23.1 (BCA) | 68.6 |
| Synthetic B | 54.0 (AAA) | 19.0 (17.9~19.9) | 73.0 |
| Synthetic C | 45.5 (44.5~47.6) | 18.6 (17.9~19.9) | 64.1 |

- Agent2 `Original ≈ B > A ≈ C`: 3회 모두 성립
- Agent3 `Original ≈ A > B ≈ C`: 3회 모두 성립
- 역할 분리 확인: A는 Agent2 점수만, B는 Agent3 점수만 떨어뜨림
- 12회 실행 전부 3단계 통과 (최저 62.4점)

### Agent2 프롬프트 수정 과정 (Agent2 총점 평균, 54점 만점)
| 버전 | Original | A | B | C | 비고 |
|---|---|---|---|---|---|
| v1 초기 프롬프트 | 50.8 | 44.5 | 40.3 | (실행별 편차 큼) | B가 가장 낮음. 형식·구성 문제를 감점 근거로 사용 |
| v2 "형식 결함은 감점 근거로 쓰지 말 것" 문장 추가 | 51.9 | 43.8 | 43.1 | 43.4 | 개선 없음 |
| v3 suitability 문구 완화 ("특히 엄밀하게" 삭제, 기준 대상 좁힘) | 52.9 | 44.2 | 45.2 | 42.4 | 소폭 개선 |
| v4 "연구 내용 vs 논문 형식 구분" 섹션 + 감점 전 분류 원칙 | 54.0 | 46.6 | **54.0** | 44.5 | B가 Original 수준으로 회복 |

v1~v3에서 B가 낮았던 이유: suitability 감점 근거가 "동일 문단 반복", "초안 메모 노출", "Fig./Table만 나열" 등 형식 문제였고, 모델이 이를 "신뢰성 저하", "논리적 완결성 부족"으로 바꿔 표현해 우회했다. v4에서 형식 문제를 그런 식으로 재표현하는 것 자체를 금지하고, 형식이 교정됐다고 가정하도록 지시했다.

## 4. 한계 및 팀 논의가 필요한 사항


1. **프롬프트의 "논문 형식" 정의에 넣은 예시가 B의 변형 내용과 겹친다** (오탈자, 절 번호·제목, 캡션, 배치 순서, 문장·문단 반복). 일반적인 형식 결함 범주이지만, README의 "합성데이터에 맞춰 프롬프트를 과도하게 조정하지 않는다"는 원칙과 충돌할 수 있어 팀 판단이 필요하다.
2.**3단계 과락 게이트(56점)는 이 데이터로 검증되지 않는다.** D등급이 정확히 70%라서 모든 항목이 D여도 통과하며, 12회 실행 모두 통과했다. 게이트 검증에는 내용 결함이 훨씬 심한 합성 논문이 필요하다.
3. **`recognition`, `topic_fit`은 변별력이 없다.** 합성데이터가 이 두 항목을 변형하지 않아 모든 논문이 A~B이며, 변별 신호는 `suitability` 하나뿐이다.
4. **실행 간 변동이 있다.** temperature 0인데도 Agent2의 A·C는 44.5~47.6점, Agent3의 B·C는 17.9~19.9점 범위에서 움직인다. 순서 관계는 유지되지만 단일 실행 결과로 판단하면 안 된다.

